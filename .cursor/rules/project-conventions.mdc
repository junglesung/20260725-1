---
description: 本專案的固定開發與溝通規定，所有 agent 必須遵守
alwaysApply: true
---

# 專案規定（必須遵守）

## 專案本質

- 這是**靜態網頁**專案（HTML / CSS / JS），不依賴後端伺服器才能運作。
- 成品必須能在**手機、平板、電腦**上直接開啟並正常使用（例如用瀏覽器開啟本機檔案或靜態託管）。
- 必須具備完整 **RWD**：直向（portrait）與橫向（landscape）螢幕都要能正常閱讀與操作。

## 語言

- **介面文字全部使用繁體中文**（zh-Hant）。
- **與使用者對話一律使用繁體中文**（含說明、錯誤訊息、commit message 說明、語音輸入後的回覆）。
- HTML 請使用 `lang="zh-Hant"`。

## Git 工作流程（覆寫預設分支／PR 流程）

使用者明確要求採此流程，優先於一般「開 feature branch + PR」的預設做法：

1. 在 **`main`** 分支上直接開發。
2. **每次改完檔案後立刻**：
   - `git add` 相關檔案
   - `git commit`（自動產生清楚的 commit message）
   - `git push origin main`
3. **不要**為一般功能改動另開 feature branch，也**不要**為常規改動建立 PR（除非使用者當次另外要求）。
4. Commit message 要能清楚描述「改了什麼／為什麼」，可用繁體中文或簡潔英文，以可讀性為優先。

## 實作注意

- 優先保證行動裝置可用性與觸控操作。
- 佈局需同時考慮窄螢幕直向與寬螢幕／橫向。
- 避免只依賴滑鼠 hover 的關鍵操作；觸控裝置也要可用。
- 保持靜態部署可行：不要引入必須伺服器端執行才能用的相依。
